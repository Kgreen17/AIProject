Objective: Write
a
function
that
checks
whether
a
given
string is a
palindrome.
A
palindrome is a
word, phrase, number, or other
sequences
of
characters
that
reads
the
same
forward and backward
(ignoring spaces, punctuation, and capitalization).

Input: A
string
that
may
contain
letters, digits, spaces, and punctuation.
Output: A
Boolean
value
that
indicates
whether
the
input
text is a
palindrome(true

           or false)

Examples:
For
text = "A man, a plan, a canal: Panama", the
output
should
be
True.
For
text = "race a car", the
output
should
be
False.
For
text = 